#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, then in uppercase.
(*
 * Return: 0 on success
 */
int main(void)
{
	char a = 'a';
	char b = 'A';

	while (c <= 'z')
	{
		putchar(c);
		a++;
	}
	while (b <= 'Z')
	{
		putchar(d);
		b++;
	}
	putchar('\n');
	return (0);
	}
