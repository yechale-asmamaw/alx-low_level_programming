#include <stdio.h>
/**
 * main - Prints except q and e
(*
 * Return: 0
 */
int main(void)
{
	char a = 'a';

	while (a <= 'z')
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);
		}
		a++;
	}
	putchar('\n');
	return (0);
}
